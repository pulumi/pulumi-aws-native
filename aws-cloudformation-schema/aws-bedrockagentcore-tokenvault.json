{
  "typeName" : "AWS::BedrockAgentCore::TokenVault",
  "description" : "Resource Type definition for AWS::BedrockAgentCore::TokenVault",
  "definitions" : {
    "KmsConfiguration" : {
      "type" : "object",
      "description" : "Contains the KMS configuration for a resource.",
      "properties" : {
        "KeyType" : {
          "type" : "string",
          "description" : "The type of KMS key (CustomerManagedKey or ServiceManagedKey).",
          "enum" : [ "CustomerManagedKey", "ServiceManagedKey" ]
        },
        "KmsKeyArn" : {
          "type" : "string",
          "description" : "The Amazon Resource Name (ARN) of the KMS key.",
          "minLength" : 1,
          "maxLength" : 2048,
          "pattern" : "^arn:aws(|-cn|-us-gov):kms:[a-zA-Z0-9-]*:[0-9]{12}:key/[a-zA-Z0-9-]{36}$"
        }
      },
      "required" : [ "KeyType" ],
      "additionalProperties" : false
    }
  },
  "properties" : {
    "TokenVaultId" : {
      "type" : "string",
      "description" : "The unique identifier of the token vault.",
      "minLength" : 1,
      "maxLength" : 64,
      "pattern" : "^[a-zA-Z0-9\\-_]+$"
    },
    "Arn" : {
      "type" : "string",
      "description" : "The ARN of the token vault.",
      "pattern" : "^arn:[a-z0-9-\\.]{1,63}:[a-z0-9-\\.]{0,63}:[a-z0-9-\\.]{0,63}:[a-z0-9-\\.]{0,63}:[^/].{0,1023}$"
    },
    "KmsConfiguration" : {
      "$ref" : "#/definitions/KmsConfiguration",
      "description" : "The KMS configuration for the token vault."
    },
    "LastModifiedDate" : {
      "type" : "string",
      "description" : "The timestamp when the token vault was last modified.",
      "format" : "date-time"
    }
  },
  "additionalProperties" : false,
  "readOnlyProperties" : [ "/properties/Arn", "/properties/TokenVaultId", "/properties/KmsConfiguration", "/properties/LastModifiedDate" ],
  "primaryIdentifier" : [ "/properties/Arn" ],
  "additionalIdentifiers" : [ [ "/properties/TokenVaultId" ] ],
  "tagging" : {
    "taggable" : false
  },
  "handlers" : {
    "read" : {
      "permissions" : [ "bedrock-agentcore:GetTokenVault" ]
    },
    "list" : {
      "permissions" : [ "bedrock-agentcore:GetTokenVault" ]
    }
  }
}