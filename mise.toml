[settings]
experimental = true

[hooks]
postinstall = "mise deps install agent-skills"

[tools]
"pipx:apm-cli" = "0.26.0"

[deps.agent-skills]
sources = ["apm.yml", "apm.lock.yaml"]
outputs = [
  "apm_modules/",
  ".agents/skills/",
  ".claude/skills/",
]
run = "apm install --frozen"
